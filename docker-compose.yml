services:
  mongodb:
    image: mongo:7
    container_name: transitops-mongodb
    ports:
      - "27017:27017"
    volumes:
      - transitops_mongo_data:/data/db
    restart: unless-stopped

volumes:
  transitops_mongo_data:
